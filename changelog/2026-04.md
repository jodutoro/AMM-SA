# AMM Toolkit — What's New (April 2026)

> This is a curated digest for agency partners — filtered and explained in terms of what actually matters for your work.

## Claude Code — v2.1.111 (April 16, 2026)

**What changed:** New `xhigh` effort level for Claude Opus 4.7, tunable via `/effort`, `--effort`, and the model picker. Auto mode is now available for Max subscribers when using Opus 4.7.

**Why it matters for your agency:** `xhigh` sits between `high` and `max` — useful when you want more reasoning than `high` gives you but don't want to burn the full `max` token budget. For the audit + content generation workflows in this toolkit, `xhigh` is often the sweet spot: deep enough to catch nuanced SEO and compliance issues, cheap enough to run across a full client portfolio without hitting usage caps. Auto mode for Max subscribers means the model will adapt effort level per task automatically — let it handle the routing.

**Action needed:** Nothing required. If you are on the Max plan, try `/effort xhigh` on your next audit run and compare output quality. If you are running this toolkit's workflows programmatically, consider adding `--effort xhigh` to your default command for the audit and content-brief steps.

---

## Claude Code — v2.1.112 (April 16, 2026)

**What changed:** Fixed a bug where "claude-opus-4-7 is temporarily unavailable" would appear under auto mode.

**Why it matters for your agency:** If you were seeing intermittent failures when running auto mode, update — this fix removes the transient unavailability error that was blocking longer sessions.

**Action needed:** Update Claude Code to v2.1.112 or later. On macOS/Linux: `npm install -g @anthropic-ai/claude-code@latest`. On Windows (WSL): same command inside Ubuntu.

---
## Claude Code — v2.1.113 (April 17, 2026)

**What changed:** Several shipped together in this release:
- CLI now spawns a native Claude Code binary (via per-platform optional dependency) instead of the bundled JavaScript.
- New `sandbox.network.deniedDomains` setting blocks specific domains even when a broader `allowedDomains` wildcard would otherwise permit them.
- Fullscreen mode: `Shift+↑` / `Shift+↓` now scroll the viewport when extending a selection past the visible edge.
- `Ctrl+A` and `Ctrl+E` move to the start/end of the current logical line in multiline input (matches readline).
- Windows: `Ctrl+Backspace` now deletes the previous word.
- Long URLs in responses and bash output stay clickable when they wrap across lines (terminals supporting OSC 8 hyperlinks).
- `/loop` improvements: `Esc` now cancels pending wakeups, and wakeups display as "Claude resuming /loop wakeup" for clarity.

**Why it matters for your agency:** The native binary change is the headline — faster cold start and lower memory overhead during long audit or content-generation runs. `sandbox.network.deniedDomains` is the one to flag for client work: if you are running sandboxed research or code execution on behalf of a client, you can now block third-party domains (analytics, trackers, known bad actors) even inside a permissive allow-list. The readline-style Ctrl+A/Ctrl+E and Windows Ctrl+Backspace fixes make multiline prompt editing feel closer to a native shell. The `/loop` Esc cancel is the practical win for anyone running autonomous loops — you can now stop a pending wakeup without killing the whole session.

**Action needed:** Update to v2.1.113. If you run workflows inside a sandboxed config, review your `sandbox.network.allowedDomains` and add `deniedDomains` entries for anything you don't want leaking client-session traffic.

---

## Claude Code — v2.1.114 (April 18, 2026)

**What changed:** Bug fix — resolved a crash in the permission dialog when an agent teams teammate requested tool permission.

**Why it matters for your agency:** If you are experimenting with Agent Teams (multi-agent collaboration mode), this fix is required — the permission prompt crash was blocking teammate spawning.

**Action needed:** Update to v2.1.114 or later. Nothing else required.

---


## SearchAtlas Platform — April 2026 Roadmap Update

**What changed:** Major platform release cycle announced with several features shipping or imminent:
- **Atlas Brain v2** — shipping end of April 2026. 2x faster with Google Search Console and social media MCPs built in. Replaces manual data-pull steps with native integrations.
- **Content Genius v2** — fully agentic rewrite. Half the cost, 2x faster, designed to run autonomously in multi-step workflows.
- **Atlas CLI** — free and unlimited, running the Atlas One LLM (trained on 1.8B rows of SA platform data). ETA ~30 days from April 13. Positioned as marketing-execution tooling built specifically for SA workflows.
- **Atlas Command** — visual agent-steering UI with mobile app, Brand Vault integration, and Research Oracle built in. For members who need to direct agents without a CLI.
- **Universal Credits** — unified credit bank across all SA products, replacing per-product limits. Per-user quota logging and agent credit caps included.
- **CloudStacks** — expanded from 14 to 35+ site slots with improved internal interlinking for index retention across satellite networks.
- **New ad channels** — Meta, LinkedIn, Uber Ads, and ChatGPT Ads added to the PPC workflow.

**Delivered this month:**
- **Research Oracle** — passive insight generation system. Setup file provided; runs continuously once installed.
- **Enterprise SEO Audit** — 6-phase workflow (GSC + GA4 + crawl). Repo access shared with members.
- **Google Ads Audit playbook** — battle-tested across 29 accounts and 925 campaigns; documented +70% conversion lift without budget increases.
- **SEO ROI Calculator** — interactive HTML sales asset for client acquisition and upsell conversations.

**Why it matters for your agency:** Atlas Brain v2 and Atlas CLI together close the research-to-execution loop. Brain v2 surfaces the signal (ranking gaps, competitor moves, GSC data), Atlas CLI runs the execution autonomously — at no per-run cost once launched. Content Genius v2 dropping cost by half makes agentic content workflows viable at portfolio scale. Universal Credits removes the per-product allocation juggling that previously forced tradeoffs between running content vs. audits vs. competitor research on the same account.

**Action needed:**
- **Install Research Oracle now** — it builds intelligence passively, so the sooner it runs the more signal you have when you need it. See the HTML setup file in shared AMM resources.
- **Run the Google Ads Audit playbook on one client** before using it in a pitch. The audit produces the report you show — run it first.
- **Monitor Atlas CLI launch (~30 days from April 13)**. Free + unlimited + Atlas One model makes this the default for high-volume SA execution tasks once available.
- **Plan satellite site builds** using the expanded CloudStacks allocation (35+ slots).

---
